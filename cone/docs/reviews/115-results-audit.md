# #115 — prod `results_v2` fidelity audit

> Generated by `node scripts/audit-results.mjs` · 2026-07-28
> Read-only preflight for [plans/52](../plans/52-result-fidelity-chain.md).

**Corpus:** 23 `results_v2` rows · 38 block entries ·
294 blocks across 50 session days.

## 1. Malformed `perfTime` (no colon) — the missing mm:ss mask

**0 of 16** logged times (0.0%) have no `:`.
`toSecs` reads these through its single-segment `parseInt` branch, i.e. **as raw seconds**.

_None._ The raw inputs have not produced a colonless time in prod yet — the mask rollout is preventive here, not a cleanup.

## 2. Non-canonical `scale` — the ClassPanel drift

Canonical is `RX · Inter · SC · Adaptado`. **0** entries carry something else.

_None._ No normalization script needed — fixing `ClassPanel` at the source is enough.

## 3. MetCon / HIIT results — blast radius of making them time-scored

**1** logged entries on MetCon/HIIT blocks:
with a time **0** · with rounds/reps **1**.

1 entries are rounds-scored and **would re-rank** when `isTimeBlock` gains these types (they have no time, so they sort last). Step 4 needs its own normalization note.

## 4. Goals — how much of the badge surface is reachable

**3** of 294 blocks carry a `goal`.

- `time` × 3

Comparable (not `text`): **3** · of those, **2** already have at least one logged result, i.e. the badge would render on 2 block(s) today.

## 5. DNF sizing (step 2)

- Time-block entries: **18**
- Already DNF-shaped (no time, has rounds): **0** 
- Time-block entries with **no score at all** (no time, no rounds): **2** — capped athletes with nothing recordable
- Time blocks with no `rounds` set: **18 of 41** (43.9%) — on these the DNF field is hidden entirely today.
