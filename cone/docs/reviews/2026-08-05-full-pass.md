# App review — 2026-08-05 (full pass · Tier 3 closeout gate)

Method: `/app-review` (9 dimensions; see [WORKFLOW.md](../WORKFLOW.md) "Review cadence"). Trigger: **[plans/68](../plans/68-tier3-closeout.md) step 6** — this pass is *part of* the last Tier 3 plan rather than a follow-up, and it is the gate that closes the housekeeping program and produces Tier 4's run order. **No code changed** — findings triaged into [BACKLOG.md](../BACKLOG.md) (rows referenced as #N). Legend: 🔴 high · 🟡 medium · ⚪ low · ✅ verified/resolved.

Fifth full pass; the [previous](./2026-07-26.md) was 2026-07-26, with a targeted two-dimension pass on [2026-08-03](./2026-08-03-criador-text-logging.md). Shipped between: Tier 2's last row (#74-C), the whole Tier 3 queue (plans/57–68 — 20 rows), and plans/67's per-box theme + `tema.html`.

**Sweeps were run by hand, not by parallel Explore agents** (the harness in this session does not spawn agents unattended). Every count below is from a command re-run today; none is carried forward.

## Method notes / what could not be checked

- **Dim 1 was driven, not skipped** — the 2026-08-03 review's lesson was that four consecutive full passes missed six live bugs because they read instead of drove. This pass drove `schedule.html` and `results.html`'s deep links and the brand-new `tema.html` at 390px against the local stack, which is where 🔴 R1 came from. It is **not** a whole-app walk: the SPA (which needs a login) was checked by code + build, not driven.
- **A live probe of prod's anon read surface was attempted and blocked by the sandbox**, so 🔴 R2 rests on the migrations + call-site evidence. ⚠️ CLAUDE.md warns prod's real policy names diverge from `0001`'s reconstruction — whoever plans R2 must confirm against `supabase db dump --linked --schema public` before assuming the policy is named `"public read"` there.
- **The "multi-session day" acceptance line in plans/68 cannot be exercised on the current seed.** Every multi-session day in the dev data pairs one untagged session with one box-tagged one, and `boxScope.js` is a **partition** — so no scope ever shows two at once. Not a bug; a note for whoever writes that test.
- `/security-review` is diff-scoped, not whole-codebase — manual pass done instead, as in every previous review.

## Resolved since 2026-07-26 ✅

- **The 2026-07-04 dual-client two-hop breach (old #41) is gone.** `slides.jsx` is client-free and `TvController.jsx` imports it directly; `criador/WeekGrid.jsx` → `index/rail.jsx` is client-free too. `createClient` appears **once** in the built SPA bundle.
- **Duplication is at zero for every canonical helper.** `fmtSecs`/`toSecs`/`exVolStr`/`rankResults`/`perfStr`/`uid`/`toISO`/`getWeek`/`deriveScale`/`blkColor`/`normExName`/`prBest` each have exactly one definition. The three surviving `fmtDate` "forks" were read and are **genuinely different formats** (`slides.jsx` adds a comma for the wall display; `events.jsx` is `dd/mm/yy` for the PDF; `Publicador.jsx`'s is a `ddmmyyyy` filename slug) — not consolidation debt.
- **Design tokens: zero undefined references**, and all four theme blocks carry **exactly 29** tokens (measured, not quoted). The 11 names that look undefined are locally set custom properties (`--fam`, `--pcol`, `--cc`, …) plus a template-literal prefix.
- **plans/65's token work verified independently:** `--border` vs `--divider` measures **1.49 / 1.50 / 1.52 / 1.50** across the four themes, and `--red`/`--err` now pass 4.5:1 everywhere (5.40 / 4.93 / 5.05 / 5.24). `--accent-text` passes on `--accent`, `--teal` and `--gold` in all 12 cells.
- **`src/public` hex is down to 35**, and every one is exempt or documented: `tema/Tema.module.css` ×20 (theme swatch previews — a preview of a palette *cannot* use `var()`), `slides.jsx` ×6 (the `getComputedStyle` fallbacks + `FAMILY_COLOR`'s own + #85's `var(--stone2, …)`), `TV.module.css` ×2, `BlockTypePicker.jsx` ×2 (data colours — but see 🟡 R6).
- **CI has all four gates**: `npm test` → `npm run lint` → `npm run format:check` → `build:all` (`deploy.yml:27-40`). A failing test, a lint warning **or** an unformatted file blocks the deploy.
- **#114 re-verified by running it, not by reading it:** `npm run design:cards` twice in a row produced byte-identical output across all 10 cards.

## 1 · Product / UX (driven at 390px, local stack)

- 🔴 **R1 — the #89 deep link lands the session 193px underneath the sticky chrome, on the page the user actually reported.** `schedule.html` stacks **three** sticky bars on mobile (`.hdr` 0–88, `.selBar` 88–140, `.weekNav` 136–193). `Schedule.jsx:187-189` scrolls the target with a bare `scrollIntoView({block:'start'})` and **no `scroll-margin-top`**, so the card's top lands at `y=0` and `document.elementFromPoint` at that point returns `.hdr`. Measured: `targetTop: 0`, sticky chrome bottom `193`. On screen the athlete arrives **mid-session** — the session name, date and entire first block are above the fold. ⚠️ **plans/68 named exactly this hazard for `Results.jsx`** ("`'start'` would slide the card under it") and fixed it there with a measured offset — `Results.jsx:222-226` sets `scrollMarginTop = header.height + 8` and lands correctly at `y=95` against an 87px header. The Schedule side got the `'start'` half without the offset half, against a sticky stack **more than twice as tall**. → **new #147**.
- ✅ **`tema.html` (shipped today, never UX-reviewed) is clean at 390px.** Four theme cards with live `--pv-*` previews, "EM USO" on the active one; picking a theme writes `cone_theme_user` **and** the legacy `cone_theme` (which the pre-paint script reads), applies the class immediately, and reveals the "Usar o tema do box / Voltar para …" reset exactly as plans/67 specified. Reset clears the key. No findings.
- ✅ **The deep link's other three halves work**: the off-week jump lands on the right week (26/07–01/08), the session is found and expanded with its blocks rendered, and `?box=` carried on the URL puts an out-of-scope session back in scope.

## 2 · Design consistency

- 🟡 **R6 — the same benchmark taxonomy is colour-coded two different ways.** `criador/TypePicker.jsx:108,115,122` paints Girls `#d8a840` gold · Heroes `#5090e0` blue · Open/Regionals `#9070d8` violet (the three CLAUDE.md documents as exempt data colours). `public/timer/BlockTypePicker.jsx:17-18` paints Girls `#d05878` rose · Heroes `#d8a840` gold — so **"Heroes" is gold in one picker and blue in the other, and gold means "Girls" in the second**. Same bug shape as #51's scale-colour reconciliation (the same thing showing a different colour depending on which page you opened). The timer's list also stops at two categories where Criador has three. → **new #148**.
- ✅ **Zero undefined tokens; 29 per theme in all four blocks** (see Resolved).
- ✅ **Contrast re-measured across 12 pairs × 4 themes.** No *new* failures. The four known ones are unchanged and remain #14's: `--gold` on light themes (3.79 totk-light · 4.49 sb-light), `--muted` on the darks (3.75 · 3.26), `--dim` in all four (2.27–3.60), `--green` on sb-light (2.93). `tema.html` introduces no new cells — its swatches are `--pv-*` locals, not palette tokens.
- ✅ **Theme-init present on every entry HTML** including the new `tema.html`. (`athletes.html` has none and correctly so — it is a redirect stub with no UI.)
- ⚪ **SPA hex is 560**, entirely inside files a queued design session already owns: the Publicador family 374 (→ #59), `Servicos.jsx` 70 + `Atletas.jsx` 18 (→ #56), the Resultados family 54 (→ #57), `tvController.module.css` 33. No unowned surface.

## 3 · Code quality

- 🟡 **R7 — `Schedule.jsx` grew 1320 → 1457 raw and the #74 watch has it in the wrong bucket.** It is now the **largest public page and the sixth-largest file in the repo**, and unlike the four files over the 1500 line it has **no queued session that owns it** — the public design program (B1–B4) shipped, and #17's ExerciseList adoption is markup, not size. The watch lists it under "under, watch only" with no owner. This pass added ~90 of those lines (#89 + #71). → **correction to the #74 watch**, not a new decomposition row yet.
- 🟡 **R3 — `calcTotal`, the only money arithmetic in the app, is an untested closure inside a 1630-line JSX file** (`publicador/events.jsx:651`). It was **changed today** (#104a — `per_hour` now bills fractional hours) with no test able to pin it, and `publicador/exportHelpers.js` — the file that exists to hold that file's pure helpers — has no test file either. Extracting `calcTotal` + the per-currency subtotal into `exportHelpers.js` makes both testable without touching the component. → **new #149**.
- ⚪ **Seven exported symbols have no consumer outside their own module/test** — `wod.js` `BLOCK_FAMILY`/`SCALE_FALLBACK`, `registry.js` `ALIASES`, `exerciseGroups.js` `ROOTS`, `resultEntry.js` `ATHLETE_KEYS`, `theme.js` `DEFAULT_THEME` (all six are deliberate extension points or test surfaces — leave them), and **`boxScope.js` `clearBoxScope`, which is genuinely dead** while CLAUDE.md advertises it in the module's API line. Clearing happens through `?box=all`/empty inside `getBoxScope`. → one line in **#73**'s family; not worth its own row.
- ✅ **No canonical-helper forks** (see Resolved). The consolidation program (#16/#83/#84/#37/#70) has held through 20 rows of Tier 3 churn.

## 4 · Architecture & contracts

- ✅ **Dual-client invariant clean at one *and* two hops** — no `utils/supabase` import under `src/public`, no `public/supabaseClient` under `src/components`, and every one of the 20 `src/components → src/public` static imports lands on a client-free module. One `createClient` in the built SPA bundle.
- ✅ **`push()` is patch-only** — `useTvSync.js:55` upserts `{ id: 1, ...patch, updated_at }`. The local `base` default is used only for optimistic local state, never sent.
- ✅ **`session.public !== false`** on all five session-rendering public pages (Index, Schedule, Results, Me, Leaderboard); `tv.html` correctly does not filter.
- ✅ **`normalizeSessionIds` at every entry point** — all six public pages plus `storage.js`'s two paths (#110's fix is intact).
- ✅ **`syncTheme` on the six pages plans/67 specified.** `tv.html`'s absence is a **recorded decision** in that plan (the wall has no box scope and is driven by TvController), not a gap.
- ✅ **Build whitelist ↔ service-worker precache consistent** — 10 built pages; `sw.js` precaches 9 of them + `manifest.json` (`tv.html` deliberately excluded, `log.html` in neither). `cache.addAll`'s atomic-404 hazard is not armed.
- ⚪ `log.html` is still unbuilt and `PresenterView`'s share URL still points at it — unchanged, still **#113**.

## 5 · Security

- 🔴 **R2 — four tables carry an anon `"public read"` policy with zero public-page consumers, and one of them is the coach's private agenda.** Mapped every anon-readable table against its actual readers:

  | Table | public-page readers | holds |
  |---|---|---|
  | `events` | **0** | `label` · `notes` · `athleteIds` · `local` · `durationMin` — the coach's schedule, incl. personal-training appointments and free-text notes |
  | `templates` | **0** | session templates |
  | `results` | **0** | the legacy v1 results blob, superseded by `results_v2` |
  | `lb_colors` | **0** | nothing — zero references anywhere in `src/` since plans/48 deleted the client leg |

  `events` is only ever reached through `src/utils/supabase.js:74` (the authenticated SPA client). This is **exactly the #81 finding shape** — business data readable by the public anon key that no public page renders — and #81 closed `coach_profile` + `locations` while leaving these four. The fix is the same two-line `DROP POLICY` per table plus a `migration repair`; nothing anon reads them, so nothing breaks. ⚠️ Verification gap stated above: prod's real policy names must be dumped first. → **new #150**.
- ✅ **No XSS sinks** — zero `dangerouslySetInnerHTML` / `eval` / `new Function` in `src/`. The single `innerHTML` is `gallery/MobileFrame.jsx:18` assigning a constant `''` to an iframe head (dev-only, never built).
- ✅ **No secrets in the client** — no `service_role`, no hardcoded URL or key; both clients read `import.meta.env`.
- ✅ **The write surface is as documented** — `is_allowed_user()` on every table, anon writes only through `class_checkin` / `log_result`, and `0008` (shipped in this plan) caps the guest branch at 20 while preserving `SECURITY DEFINER` / `search_path` / grants. Re-verified live through the anon key on the local stack: 25 guest taps → 20 rows, distinct names still both land, athlete branch still dedupes, anon direct UPDATE still refused.

## 6 · Performance

- ✅ **Healthy; nothing regressed.** SPA main **428 kB**; the heavyweights are all lazy Publicador-only chunks (`jspdf` 392 · `html2canvas` 196 · `index.es` 148 · `autotable` 32 · `purify` 28), confirming plans/39's split still holds. Largest tab chunk `Criador` 128 kB.
- ✅ **Public pages**: shared `supabaseClient` 200 kB + `jsx-runtime` 188 kB, then 12–56 kB per page (`schedule` 56 · `me` 48 · `tv` 44 · `timer` 32 · `results` 28 · `index` 16 · `leaderboard` 12).
- ⚪ Startup fetches are 4–7 per public page but issued in **one `Promise.all`**, so it is one round-trip of latency, not seven. The old #22 consolidation idea buys little; `tema.html` needs only 1.

## 7 · Accessibility

- ⚪ **Both TV-surface items filed in the 2026-07-04 pass are still open and were re-confirmed live** (they belong to **#14**, not new rows): `ClassPanel.jsx:159` is a `<div onClick={onToggle}>` accordion header and the file contains **zero** `role`/`tabIndex`/`onKeyDown` anywhere, so the class roster cannot be opened from a keyboard; `slides.jsx:382`'s countdown pill still has no `aria-live`/`role="timer"`. ⚠️ Worth recording the nuance when #14 is planned: the countdown renders on a gym wall with no screen-reader user, so it is the *lowest*-value item in that row, while ClassPanel is coach-facing SPA and is the highest.
- ⚪ **Landmark/heading gaps**: no `<main>` in `Index.jsx`, `Timer.jsx`, `TV.jsx`, `Recover.jsx`; no `<h1>` in `Index.jsx`, `Me.jsx`, `Tema.jsx`. `Index.jsx` — the landing page — has neither. → **#14**.
- ✅ No icon-only button without an accessible name survived the pattern sweep (0 matches for a button whose sole child is an icon and which lacks `aria-label`).

## 8 · Testing & gates

- ✅ **748 tests across 21 files**, up from 704/19. All four CI gates live (see Resolved).
- 🟡 **The untested set now maps cleanly onto the risk hotspots** — and one of them is money (R3, above). Modules holding pure logic with no test file: `publicador/exportHelpers.js` (+ `events.jsx`'s inline `calcTotal`), **`results/resultsHelpers.js`** (whose named mirror `schedule/scheduleHelpers.js` gained tests today, so the pair is now asymmetric), `lib/blobTables.js`, `lib/benchmarks.js`, `hooks/useGroupRotation.js`, `hooks/useLiveRegistration.js`. The last two are **#23**'s existing targets and have been for four reviews. → fold `resultsHelpers` into **#23**; `calcTotal` gets its own row (R3/#149) because it needs an extraction first.

## 9 · Docs & process hygiene

- 🟡 **R4 — the board's stale-marker failure has now happened a third time, and this pass found four distinct shapes of it.** BACKLOG.md's own header warns about this twice; WORKFLOW.md:50 records that it once cost a whole session. Current state, measured:
  - **Six rows still lead with `🟢 [→ Ready · plans/NN]` for a plan that shipped** — #119/plans/58 and #124/plans/59 (2026-08-03), #98/plans/50, #108/plans/51 and #24/plans/49 (2026-07-27), #26/plans/35 (2026-07-21). Each is marked shipped *elsewhere* on the board, so a reader scanning for green picks sees six false ones.
  - **Three icebox rows shipped 2026-08-04 by plans/66 are still bare** — #134, #135, #136 at `BACKLOG.md:277-279`. #137 immediately below them carries its ✅. They were missed as a group.
  - **`plans/61` has no Done marker at all** though all three of 61·A/B/C shipped — and WORKFLOW.md's plan-lifecycle rule is precisely *"no marker = actionable"*, so `plans/` currently presents the largest Criador plan as live work.
  - **`plans/52`'s marker reads "Step 1 (#115) Done"** while the whole #115→#118→#112→#117→#116 chain completed 2026-07-30.

  Fixing these by hand for a third time is not the fix. → **new #151**: a ~20-line check script (`scripts/audit-backlog-markers.mjs`, same shape as the existing `audit-*` scripts) that cross-references row markers against plan Done markers, runnable in the session ritual. → **the 10 rows themselves are corrected inline in this session**, since they are pure doc drift.
- 🟡 **R5 — CLAUDE.md drifted from plans/67, which shipped hours before this review.** All pure doc drift, **fixed inline this session**: the public-pages table has 9 rows and is missing `tema.html`; the page-whitelist note says "9 pages" where `vite.public.config.js` now lists **10**; `src/public/lib/theme.js` (`syncTheme`/`resolveTheme`, which killed three divergent copies of the theme list) is absent from the "Shared utilities" section entirely; `settings.value.boxThemes` and the `cone_theme_user` key are undocumented. Also stale independently of plans/67: the test count (**704/19 → 748/21**) and the migration list (missing `0008`).
- ⚠️ **Both R4 and R5 are the same root cause** — WORKFLOW.md's "Docs are part of Done" plus ritual steps 4–5, skipped again. R5 is the second consecutive session to ship a feature and leave CLAUDE.md behind.

## Triage summary

| # | Finding | Sev | Disposition |
|---|---|---|---|
| R1 | Schedule deep link lands 193px under the sticky stack | 🔴 | new **#147** |
| R2 | 4 tables anon-readable with 0 anon readers; `events` is the coach's agenda | 🔴 | new **#150** |
| R3 | `calcTotal` (the only money code) untested + unextractable | 🟡 | new **#149** |
| R4 | Board/plan marker drift, third occurrence | 🟡 | new **#151** + 10 rows fixed inline |
| R5 | CLAUDE.md drift from plans/67 + counts | 🟡 | fixed inline |
| R6 | Benchmark-category colours disagree between the two pickers | 🟡 | new **#148** |
| R7 | `Schedule.jsx` 1457 raw, no owning session | 🟡 | correction to the **#74** watch |
| — | ClassPanel keyboard · countdown `aria-live` · landmarks | ⚪ | **#14** (re-confirmed, nuance added) |
| — | `resultsHelpers.js` untested | ⚪ | **#23** |
| — | `clearBoxScope` dead but documented | ⚪ | **#73** family |

## Tier 4 run order

Tier 3 closed with this plan. The housekeeping program is **complete** — Tiers 1, 2 and 3 are fully shipped — so Tier 4 is no longer "leftover housekeeping"; it is the point where **the design program becomes the queue**, as the board has said it would. Ranked:

1. **#147** (R1) — a live regression in a fix that shipped today, on the exact flow the user reported. S · Sonnet. **First, and it should not wait for a batch.**
2. **#150** (R2) — reduces the public read surface to what public pages actually use; the fix is four `DROP POLICY` statements, but it needs a prod policy dump first. S · Sonnet.
3. **#95** — the design program's first pick. **Confirmed plannable cold** (below).
4. **#151** (R4) — cheap, and it protects every session after it.
5. **#149** (R3) + **#104(c)** — the billing pair: extract and test `calcTotal`, then rate history on top of a tested function.
6. **#96** — Exercícios' pane proportions; the other measured layout row.
7. **#144 + #145 + #146** — the user's own mobile report, one file each on `results.html`/`me.html`. ⚠️ #145 ships with #146.
8. **#14** — the a11y batch, now with a measured contrast table and this pass's four confirmed items.
9. **#148** (R6), **#141**, **#113**, **#66** — the XS/decision tail.
10. **#93 + #138**, **#88**, **#23** — carried from the old Tier 4.
11. **#82**, **#29** — unchanged Tier 5.

### Is #95 plannable cold? — **Yes, verified against live code today**

Every load-bearing claim in the row still holds: all five state buttons are still in `App.jsx`'s topbar (`:424` Sair · `:443` Sincronizar · `:467` Limpar · `:475` Carregar · `:529` Salvar), `.topbar-right` is still `flex-wrap:wrap` (`index.css:18`) which is the mechanism behind the 226px, and the design is settled by explicit coach decisions (relocate three buttons into a Configurações "Dados" card, one-row mobile topbar, nothing deleted). **Two corrections to record when the plan is written:**
- The row calls the coupling `.tab-bar{top:44px}`. The `44px` hardcodes actually live on **`.sync-conflict-banner` and `.rp-sticktop` (`index.css:798-799`)**, not on `.tab-bar` itself. The coupling is real; the selector name is wrong.
- **`--spa-sticky-top` already exists** (`index.css:37` = 93px, `:793` = 49px at ≥768px) and is the token those hardcodes should collapse onto — the same token #144 notes no public page uses either. Plan #95 and #144 to agree on it rather than entrenching a third set of literals.
- The **226px figure predates plans/67's Config changes** — re-measure at 390×844 before quoting it, per this board's standing rule.

## Process notes for next review

- **Driving beat reading again.** Every 🔴 in this pass came from either driving the app (R1) or from cross-referencing a table's policy against its actual call sites (R2) — neither would have surfaced from a code read, and R1 is a same-day regression in a plan that passed tests, lint, build and format. Keep dim 1 in every full pass.
- **A fix applied to one of two sibling pages is the recurring shape.** R1 (Results got the offset, Schedule didn't) joins #144 (Schedule has sticky bars, Results doesn't) and #51/R6 (one taxonomy, two colour codings). When a plan names two files, check the second one landed.
- **R4 will keep recurring until it is mechanical.** This is the third occurrence and the second review to spend time re-deriving it; #151 exists so the fourth review doesn't.
