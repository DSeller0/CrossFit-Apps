# Cone — Full review pass · 2026-07-18

**Trigger:** Ready column emptied (#76 → #62 → #53 all shipped) **and** an L item (#53) shipped — both cadence triggers fired (WORKFLOW.md "Review cadence").
**Scope:** Full 9-dimension pass. Dims 2/3/4/7 ran as parallel Explore sweeps; 1/6 driven live; 5 manual + grep; 8/9 direct reads.
**Environment:** local Supabase stack already up (14h), public dev server @5180, Playwright walk at 1280 + 390. Build + tests run locally.

## Verdict — healthy, no blockers

The app is in its best structural shape yet: **all 8 architecture invariants PASS**, security surface is sound, **230/11 tests green**, both builds clean, and the UX walk found **zero new live bugs**. The #50–#53 design program has genuinely cleaned the *public* pages — public radius violations are now **0**, FOUC is **0/9**, and index/recover/timer hex is essentially closed. The remaining debt has **migrated into the SPA** (`src/components/**`): hardcoded colors are *growing* there, and 100% of the accessibility gap now lives there. Nothing here is urgent; this pass refills Icebox and corrects stale rows. **Ready is genuinely empty** — a short planning pass is the next step to promote 2–3 rows.

---

## Dimension 1 — Product / UX walk (driven live)

Drove all 8 public pages at 1280 + index at 390. **0 real console errors** anywhere (the lone `favicon.ico` 404 is dev-server noise — production pages link their own icons).

- ✅ **index** (#53) renders correctly desktop + mobile: full-width Sun-start week grid, expandable session card, ranking panel, dated warnings strip (3 desktop / 1 mobile), Tabler nav, PWA banner. The #18 "dead right half" is resolved.
- ✅ **Nav mobile bottom bar** (#53 fix verified live @390): 2-row layout, all 7 destinations appear **exactly once** — no Timer duplication, no "Mais" overflow double-offer.
- ✅ schedule (#50) — correct block-family colors (green/blue/amber/red), week nav, WOD blocks. results (#51) — KPI grid correctly shows **"—" for RPE médio** (no fabricated data, #75), canonical scale colors. me/Bruna (#52) — single-column, TallyBar PR gauges, RPE "—". timer config clean. tv — webfont loads (#69 verified), red ring "TIME!" state.
- 🔵 low | `me.html` desktop empty-state pane is a large dead area with one centered line — cosmetic; populated when an athlete lands via `?id=`.
- info | tv showed a stale `NENHUM EXERCÍCIO` timer state — leftover test `tv_state` in the DB, not a bug.

## Dimension 2 — Design consistency (sweep)

- ✅ **FOUC clean** (theme set in `<head>` before paint on all 9 pages). ✅ **Token drift clean** (30 tokens × `:root` + 4 themes). ✅ **Public radius clean** (0 non-circle violations — #53 closed it).
- design | med | `src/public/tv/slides.jsx:241` | `stroke="#1e1a16"` (=`--stone2` frozen) → TV progress-ring **track stays dark on both light themes**. Public-facing; the one token bug #53 Part A's TV sweep missed. **NEW → #85.**
- design | high | `src/components/tabs/Atletas.jsx:14-20` | 7 named consts hard-freeze the **entire totk-dark palette** (`BG/STONE/DIV/CREAM/SUB/MUTED/DIM`) → renders totk-dark colors in all 4 themes. Same bug class as retired athletes.html. **→ fold into #56 (Atletas design pass).**
- design | med | **SPA hex is GROWING** — `Criador.jsx` 84→**102** (+19 rgba), `Servicos.jsx` 64→**75** (+9 rgba); `index.css` 284, `Resultados.jsx` 34, `tvController.module.css` 29. **count-update → #15.**
- design | low | `Publicador.jsx` 305 hex + **45 `rgba()`** assumed jsPDF-exempt — but rgba isn't jsPDF's usual API; spot-check that none are non-exempt on-screen preview UI. **note → #59.**
- Confirmed exempt (not flagged): `blkColor`/`SCALE_COL` data colors, gallery mock data, black modal scrims, `BlockTypePicker` category accents.

## Dimension 3 — Code quality (sweep)

- code | high | `Publicador.jsx:287,364,620,1729` | inline `monthNames=['Janeiro'…]` re-declared **4×** — the file **already imports** canonical `MONTH_PT` (and re-aliases it twice). **NEW → #83.**
- code | high | `utils/config.js:2` (`DSHORT`) + `Publicador.jsx:578` + `Criador.jsx:1418` (`WEEK_DAYS`) | three `['DOM'…'SAB']` forks of canonical `DAY_PT` (`week.js:3`). **NEW → #83.**
- code | med | block-meta string (`rounds · CAP` join) hand-rolled at **~11 sites** (Publicador ×5, Resultados, BlockDetail, WodBlockCard, slides ×2, rail) with casing/order drift; nearest canonical `resultsHelpers.js:20 blkMeta` is folder-scoped. **NEW → #84.**
- code | med | session-name resolution reimplemented at **~9 sites** with divergent fallbacks ('Treino'/'Sessão'/'–'); `resultsHelpers.js:16 sessName` is folder-scoped. Needs a fallback-string decision. **NEW → #84.**
- code | low | `timer/Timer.jsx:11` ≡ `tv/slides.jsx:10` (`MODE_LBL` byte-identical twin); two Sunday month-grid builders (`Publicador.jsx:50` / `Resultados.jsx:21`); `Publicador.jsx:1104 BLOCK_C` (verify vs `blkColor` before collapsing — likely intentional). **NEW → #83/#84.**
- **Files > 800:** `Publicador.jsx` 2143 (#25), `Criador.jsx` **2049** (#26, was ~1950), `Gallery.jsx` **1351** (#74, was ~1247, still growing), `Resultados.jsx` 924 (#74). No new files crossed the line (`Atletas.jsx` 787, `timer/Timer.jsx` 783 nearest). **count-update → #26/#74.**

## Dimension 4 — Architecture & contracts (sweep)

**All 8 invariants PASS — zero violations.** Two Supabase clients disjoint (no bundle imports both, no hardcoded creds); `push()` patch-only (every key a real `tv_state` column, `timer_block_id` never `block_id`); three render paths in sync (TV BlockCard/TimerSlide share `ExerciseList`; ExRow via canonical `exVolStr`/`fmtIntensity`); `exVolStr`/`fmtIntensity` canonical-only; `s.public!==false` filter on all 5 session pages; `shared/**` free of the `ti` webfont; whitelist has all 9 pages; TV subscribes `postgres_changes` on `tv_state`.

## Dimension 5 — Security (manual + grep)

- ✅ **XSS/secrets clean:** no `dangerouslySetInnerHTML` in product code (only hit is the dev-only gallery iframe `MobileFrame.jsx:18`); **zero** `service_role`/JWT literals in `src/`.
- ✅ **RLS sound:** anon writes go only through `class_checkin`/`log_result` SECURITY DEFINER RPCs (`0003`); `log_result` never touches `coach_note`/`flag_for_review`; `coach_profile`/`locations` reads locked to the coach (`0006`/#81).
- Open surfaces (already tracked): **#71** `class_checkin` guest branch is unbounded + no-dedupe (anon can spam `anon_names`); **#82** hidden `public:false` sessions unprotectable in a single JSONB blob. No per-athlete identity means `log_result` lets anon upsert any athlete's row — inherent to the no-auth model (#30/#31). **No new security finding.**

## Dimension 6 — Performance (build)

- SPA eager entry `index-*.js` **424 kB / 122 kB gz** — coach-only (behind login), acceptable. jsPDF (399 kB) + html2canvas (199 kB) both ride the lazy `Publicador` tab ✅.
- Public pages carry a **~114 kB-gz React+Supabase vendor floor** + per-page JS (schedule 61, me 42, tv 41, results 31, timer 30, index 13, leaderboard 10, recover 3 kB). Reasonable.
- perf | low | **Crimson Pro dead payload** — 400-normal + 400-italic + 600 (latin + latin-ext, woff + woff2) built into both bundles, for **one** live usage (`Schedule.module.css` `.deskIdleHint`, italic-400). **matches #73 font note — confirmed at build level.**

## Dimension 7 — Accessibility (sweep)

- a11y | high | SPA `src/components/**` | **41 icon-only buttons with no accessible name** (Atletas 15, Criador 11, Servicos 4, Exercicios 4, Resultados 2, TvController 2, ClassPanel 1, Publicador 1, IntensityInput 1) + **29 click-`<div>` controls with no keyboard support** (+14 modal backdrops). SPA-wide: **0 `aria-label`, 0 `role=`, 0 `<main>`, 1 `<h1>`** (LoginScreen only). **refine → #14.**
- a11y | high | `Publicador.jsx` + `PresenterView.jsx` use `React.createElement` (not JSX) → **entirely missed by the 2026-07-16 JSX-only census.** Adds 6 click-controls + 2 icon buttons. **Key correction → #14.**
- a11y | high | `timer/Timer.jsx`, `tv/TV.jsx` | live countdowns have no `aria-live`/`role="timer"` (0 codebase-wide), no `<main>`/`<h1>`. **matches #14.**
- ✅ No public-page regression (7 `<main>`, ~9 `aria-live` intact; `ExRow`/`RankList`/`Results` compliant).

## Dimension 8 — Testing & gates

- ✅ **230 tests / 11 files green.** CI (`deploy.yml`) gates deploy on `npm test` + `build:all`. **No lint gate** (#32, lint still fails 155) and **no format gate** (#24).
- test | low | `npm test` **fails on Node 24** (this machine, v24.14.1) with the default worker pool — `describe` throws `reading 'config'`, "no tests" run. Passes with `--pool=forks`/`threads`. CI runs Node **20** so it's unaffected, but a contributor on current Node hits a confusing green-CI/red-local split. Fix: pin `pool` in `vite.config.js` test block (or `engines`/`.nvmrc`). **NEW → #86.**
- Coverage vs risk: the two largest files (`Publicador` 2143, `Criador` 2049) still have **0 tests** — tracked under #23/#25/#26.

## Dimension 9 — Docs & process hygiene

- ✅ Verified accurate: CLAUDE.md "230 tests / 11 files"; `vite.public.config.js` 9 pages; migrations `0001`–`0007` (7 files).
- docs | med | **Board carried stale status on shipped items.** #53 was still marked `🟢 [→ Ready · plans/31]` and #76 `🟢 [→ Ready · plans/29]` though both are in **Done** (#62 was already a correct `✅ shipped` capture-chain pointer). **Fixed inline:** flipped #76/#53 to `✅ shipped 2026-07-18 (see Done)` archived-in-place pointers (per the #62 precedent + WORKFLOW plan-lifecycle "archive in place with a Done marker"), and rewrote the Ready section to `_(empty)_` with the review note.
- docs | low | #53's 4-page hex/radius census is now historical (index/recover 0 hex, public radius 0). #15's pointer should record that the **public side is essentially clean and the debt is now SPA-concentrated and growing**. **fixed inline → #15.**

---

## Backlog triage

**New Icebox rows:** #83 (mechanical duplication quick-wins), #84 (promote block-meta + session-name formatters to `wod.js`), #85 (TV ring-track frozen token — public light-theme bug), #86 (pin vitest pool for Node 24+).
**Corrections to existing rows:** #14 (refined a11y counts + `React.createElement` discovery), #15 (SPA-concentrated + growing), #26 (Criador 2049), #74 (Gallery 1351), #56 (fold in Atletas palette-freeze), #59 (Publicador rgba spot-check).
**Fixed inline (doc drift):** flipped the mis-marked `→ Ready` status on shipped #76/#53 to `✅ shipped` archived pointers; Ready section rewritten to `_(empty)_` + review note.
**Next step:** a short **planning pass** to write plans for 2–3 top items and promote them to Ready (the review does not promote).
