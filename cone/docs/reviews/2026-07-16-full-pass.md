# App review — 2026-07-16 (fifth full pass)

Trigger: **Ready emptied** after the #52/B3 chain closed (`558b1d3`), plus #61 and #68 shipping the same day. Last full pass: 2026-07-05; the 2026-07-09 pass was design-only. Since then: #49, #50, #17, #51, #52, #61, #68 + plans/21 — a long, code-heavy gap.

**Environment:** local Supabase stack (Docker) + Playwright, both live. Docker died mid-walk and was restarted; dim 1 is **partial** as a result (see below).

Docs-only. No product code changed. All test data written during dim 5 was reverted (verified: `class_executions` back to 0 rows).

---

## Headline

**🔴 `tv.html` — the gym wall display — has been rendering in Times New Roman.** Proven live, not inferred: `src/public/tv/main.jsx` is the **only** one of the 9 public entries missing `import '../../fonts.js'`. `TV.module.css:9` correctly specifies `font-family: var(--font, …)` → `Cinzel, serif`, but no `@font-face` is ever registered, so it falls through to generic serif.

| Page | Cinzel faces loaded | Total faces | Rendered |
|---|---|---|---|
| `leaderboard.html` | **12** | 23 (Cinzel/Crimson Pro/Amarante) | Cinzel |
| `tv.html` | **0** | 1 (tabler-icons only) | **Times New Roman** |

The `'Arial Black'` fallback in `TV.module.css:9` is dead — `--font` always resolves, so the `var()` fallback never fires. One-line fix; it should not wait for #53/B4.

---

## Dimension 1 — Product / UX walk (⚠️ PARTIAL)

Drove index / schedule / tv / leaderboard at 1280×800 against the live local stack. **Docker Desktop stopped mid-walk**; it was restarted and the stack recovered, but the mobile (390) pass and the results/me/timer journeys were not re-driven. **The remaining walk is the one thing this pass owes** — dim 1 is historically where the findings the sweeps cannot see come from.

- 🔴 `tv.html` serif fallback — see Headline.
- 🟡 `index.html` @1280: content column stops at ~790px; the right ~490px is dead. Known — #53 / retired #18. Confirmed still live.
- 🟢 Console clean across all pages driven — the only error anywhere is a `favicon.ico` 404.
- 🟢 Seed data is stale (sessions end 2026-07-14, today is 07-16), so index's "Ontem/Hoje/Amanhã" render empty. **Not a bug** — data age, not code.
- 🔵 `favicon.ico` 404s on every page except `index.html` — only the root `index.html` declares `<link rel="icon">`. Cosmetic; real in prod too.

## Dimension 2 — Design consistency

- 🔴 **`tv.html` loads no webfont** — `src/public/tv/main.jsx` missing `import '../../fonts.js'` (all 8 other entries have it at line 3). See Headline.
- 🔴 **`TV.module.css` white-alpha overlays break both light themes** — `:77` `rgba(255,255,255,.05)`, `:268` `.07`, `:272` `.03`. Exactly the bug class CLAUDE.md:101 records as fixed for `TallyBar`'s baked-in `rgba(13,11,9,.65)`. `:14` `background:#000` on the blank slide is **deliberate** (comment says "reads as screen-off on every theme") — not a finding.
- 🔴 **`Index.jsx:16` is a 5th divergent block-family taxonomy.** `BLOCK_COLORS = { wod:'var(--teal)', warm:'var(--gold)', str:'#c87850', skill:'#9070d8', core:'#70a870', mob:'#5090d8' }` vs canonical `blkColor`/`FAMILY_COLOR` (`lib/wod.js:12`): index paints **WOD teal** where canonical paints it **red**, **Força `#c87850`** where canonical paints **blue**. Its taxonomy (`str/skill/core/mob`) isn't wod.js's red/amber/blue/green either, and its type sets miss `Cardio`/`LPO`/`HIIT`/`MetCon`/`Acessórios` — `Cardio` falls through to `skill` (violet) on index, green everywhere else. **The same block is a different color on the landing page than on every other page** — the precise bug #51/#52 fixed for `SCALE_COL`. (Note: `Index.jsx:10` *does* import canonical `WOD_TYPES` and Set-wraps it — that half is correct.)
- 🟡 **QR codes hardcode the dark palette** — `slides.jsx:56` `{dark:'#f0e8d0', light:'#00000000'}`, `:412` `{dark:'#f0e8d0', light:'#0d0b0900'}`. Those are totk-dark's `--cream`/`--bg`. On both light themes this paints cream-on-transparent over a light background → low-contrast, potentially **unscannable**. Canvas can't take a CSS var; needs a `getComputedStyle` read.
- 🟡 **`src/App.css` is dead** — imported by nothing (0 hits), a Vite-template leftover, and it holds **all 5** undefined tokens in the codebase (`--accent-bg`, `--accent-border`, `--text-h`, `--social-bg`, `--shadow`). Deleting the file zeroes the undefined-token category outright.
- 🟡 `--lb-font` is a **dead token** — defined in all 5 blocks of `themes.css` (`:28,40,52,64,76`), 0 usages. (`--sc-font` is live.)
- 🟡 `Timer.module.css:80,81,82` — `color:#000` on `.btnStart`/`.btnResume`/`.btnReg`; should be `var(--accent-text)` (`#000` in totk-dark, `#fff` in totk-light, `#09070f` in spirit-blossom).
- 🟡 **Tokens written out in decimal `rgb()`**, freezing totk-dark values across all 4 themes: `Timer.module.css:63,92` (=`--divider`), `:65,134` (=`--teal`); `Index.module.css:8` (=`--gold`), `:30` (=`--teal`); `TV.module.css:196` (=`--gold`), `:127,133,134,135` (=red family). `Results.module.css:65` and `RankList.module.css:23` carry **comments recording this exact bug already being fixed there** — the pattern survived in timer/index/tv.
- 🟡 `BlockTypePicker.jsx:10,14` `#d05878` + `:15` `#d8a840` — undocumented data colors outside the 4-family taxonomy. **`Benchmark` is amber in `wod.js:9` but `#d05878` here** — same block type, two colors depending on which UI you open.
- 🔵 `'Courier New', monospace` hardcoded 5× in `TV.module.css` (`:114,128,205,221,262`). No `--font-num`/`--font-mono` token exists publicly, though `src/index.css` references a `--f-num` — the SPA has a numeric-font concept the public TV doesn't share. → `#54` (C0) already owns the `--font-mono` decision.
- 🔵 `rgba(0,0,0,…)` scrims — 8 sites, 4 different alphas (.55/.6/.7/.72), no `--scrim` token: `Nav.module.css:2`, `Results.module.css:174,188,191`, `Schedule.module.css:161,181,351`, `BlockTypePicker.module.css:4`.
- 🔵 `index.html:7` `<meta name="theme-color" content="#4ac8c0">` hardcoded to totk-dark's teal; browser chrome won't follow the other 3 themes. Only index.html has this meta.

**Censuses (current):**
- **Public hex/rgb: 62 raw lines → ~39 real violations** (was ~93). Clean: `leaderboard/**` (0 of any kind), `me/**`, `shared/**` (comments only), `results/**` (3 scrims only). Dirty: **`tv/**` 17 (worst), `timer/**` 11**, `index/**` 3, `schedule/**` 3 scrims, `Nav` 1, `recover/**` 1.
- **Full `src/**` hex: 912 lines** (was ~986). Publicador 305 (jsPDF-exempt), `index.css` 284, Criador 84, Servicos 64, Resultados 34, `config.js` 30, `tvController.module.css` 29. **Half the SPA debt is 2 files** (`index.css` + `tvController.module.css` = 313). ⚠️ SPA-side lines were **not** classified into violation/data-color/comment — `~607 non-exempt` is an **upper bound**, and `config.js`'s 30 are likely mostly exempt palettes.
- **Public border-radius: 38 raw / 17 circles / 19 real violations.** `TV.module.css` 7, `Timer.module.css` 5, `Recover.module.css` 4, `Nav.module.css` 2, `Index.module.css` 1. **Zero `999px` pills remain in public.** Worst: `Recover.module.css:4` (12px), `TV.module.css:190,257` (10px). `Nav.module.css`'s 2 render on all 5 main pages — most-seen. Full-src: **311 raw / 43 circles / 268 non-circle** (was 230/27 — **grew**).
- **Tokens:** all 4 `html.theme-*` + `:root` define **exactly the same 29 tokens** — zero per-theme gaps, verified programmatically. **`var(--card)`: 0 usages** — CLAUDE.md:129's rule is stale.
- **Theme init:** **9/10 correct**, identical inline IIFE before the stylesheet. `athletes.html` has none — **correct by design** (#52 redirect stub, empty body, `location.replace()` before paint). No FOUC anywhere.
- **Fonts:** Cinzel 400/500/600/700/800/900 — **all 6 loaded, all 6 used, no gap either direction**. Amarante 400-only + synthesized bolds = as designed. **Crimson Pro 600 is loaded but effectively unused**: `--font-body` has exactly **one** live usage app-wide (`Schedule.module.css:297` `.deskIdleHint`, italic, no weight → 400-italic only). `@fontsource/crimson-pro/600.css` is dead payload on every page.

## Dimension 3 — Code quality

**Duplication — new/still-open forks.** The pattern: #16/#37/#48/#51/#52 canonicalized the **public** side; the **SPA never adopted any of it**.

- 🔴 **`WOD_BLOCK_TYPES` (`Resultados.jsx:18`) = 6 entries; canonical `WOD_TYPES` (`wod.js:3`) = 8.** Missing `'Benchmark'` and `'Estações'`. **This is the exact bug the 2026-07-02 quick-wins batch fixed** in Index/Me/Results/Athletes/Leaderboard — the SPA coach view was missed and still doesn't treat Benchmark/Estações blocks as WOD blocks. Behavioral divergence, not cosmetic. Same class as #61(d)'s `isTimeBlock`.
- 🔴 **`rowToResult` (`utils/resultMappers.js:1`) is a byte-identical fork of `mapResultRow` (`blobTables.js:9`)** — same 10 fields, same order. CLAUDE.md:114 claims `mapResultRow` canonical since #51; the SPA copy was never folded in. A new `results_v2` column must be added in **2** places today.
- 🔴 **`getTargets`/`matchesAthlete` × 3** — `utils/storage.js:69,73` (SPA) · `me/meHelpers.js:21,26` (tested) · **`schedule/Schedule.jsx:207` — a third, local, untested copy**. No canonical home exists in `lib/`.
- 🔴 **`deriveScale` re-inlined in the SPA, diverged** — `Resultados.jsx:761-769` re-declares `SCALE_RANK`/`SCALE_NAMES` verbatim *inside a useMemo* and re-inlines the min-rank loop. Canonical at `wod.js:39-47`; `Resultados.jsx:9` **already imports from `wod.js`** — it just doesn't import this.
- 🟡 **`prBest` 4th copy** — `Schedule.jsx:17-20` **drops the `pr.type==='time'` branch** the canonical `goals.js:3` has. Currently masked because its only caller (`autofillRm`, `:38`) pre-filters `type==='load'`. A latent trap.
- 🟡 `SCALES` ×3 (`wod.js:23` canonical · `Resultados.jsx:14` · `scheduleHelpers.js:6` as `LOG_SCALES`) **+ a 4th inline literal** `Resultados.jsx:774`, itself duplicating `ScaleFilter.jsx:4`'s `FILTER_SCALES`.
- 🟡 `Resultados.jsx:19` `PT_MONTHS` = verbatim `week.js:1 MONTH_PT`; `:20` `DAY_NAMES` = verbatim `week.js:4 DAY_PT_TITLE`. File already imports `toISO` from `week.js:10`.
- 🟡 `TIMER_TYPES` identical in `TvController.jsx:23` and `useTimer.js:3`; TvController already imports the hook.
- 🔵 `Publicador.jsx:850` local `fmtDate` uses `toLocaleDateString('pt-BR')` — `week.js:29-31` records that locale-formatting was deliberately removed elsewhere to stop browser-locale drift; this site kept it. `:1635` has a second, different local `fmtDate`. (`slides.jsx:43` is **not** a finding — it wraps canonical exports, explicitly sanctioned.)
- Known-open, re-confirmed: `buildProgressionLines()` still hand-rolls grouping (#45).

**Files > 800 lines (exact):**

| Lines | File | Tracked? |
|---|---|---|
| 2143 | `components/tabs/Publicador.jsx` | #25 (says ~2152 → **-9**) |
| 1950 | `components/tabs/Criador.jsx` | #26 (says ~1953 → **-3**) |
| **1247** | `public/gallery/Gallery.jsx` | **untracked** |
| **928** | `components/tabs/Resultados.jsx` | **untracked** |

Next-largest all under: Atletas 788, Timer 776, Schedule 691. Total `src/`: 18,436 lines / 124 files.

**Unused exports (verified, zero refs anywhere incl. tests):** `utils/config.js:1` `DAYS`; `:41` `DEFAULT_TYPES`; `:42` `TYPES`. **Export-only (noise, not dead):** `storage.js:28-37` — 10 `LS_*` constants + `LS_KEY:27`, `markSessionsSaved:20`; `supabase.js:12,22` `dbLoad`/`dbSave`; `wod.js:25` `SCALE_FALLBACK`, `:33` `SCALE_SHORT`; `week.js:5` `DAY_PT_FULL`.

**Dead CSS — ⚠️ CANDIDATES ONLY.** Dynamic class construction (`styles[\`pod${n}\`]`, `styles[size]`) was **not** ruled out. Leads: `TV.module.css` 21 (the `exList`/`timerEx*` sets — plausibly real, exactly what `ExerciseList` replaced), `Results.module.css` 13, `Timer.module.css` 13 (`bm*` likely superseded by `BlockTypePicker.module.css`). **`RankList.module.css` `pod1/2/3` and `TallyBar.module.css` `lg`/`sm` are near-certain false positives** — verify before touching.

## Dimension 4 — Architecture & contracts

| # | Invariant | Verdict |
|---|---|---|
| 1 | Two Supabase clients never co-bundled | **PASS** — full transitive trace |
| 2 | `push(patch)` patch-only | **PASS** — every call site |
| 3 | Three render paths in sync | **FAIL ×4** |
| 4 | Schema vs code | **PARTIAL** — `tv_state` exact; shapes not diffed |
| 5 | `session.public===false` hidden | **PASS (code)** — doc count stale |
| 6 | `BLOB_TABLES` positional | **PASS** |
| 7 | Other contracts | **NOT REACHED** |

- ✅ **Invariant 1 PASS.** #41's vector is closed and stays closed: `TvController.jsx:6`/`Criador.jsx:3` import `public/tv/slides.jsx`, whose *complete* closure is `lib/wod.js` (leaf), `lib/week.js` (leaf), `shared/ExerciseList.jsx`→`wod.js`, + qrcode/react/CSS. **No `supabaseClient.js`.** Reverse direction clean (0 hits for `utils/supabase`/`src/components` anywhere in `src/public`). Dynamic imports checked too.
- ✅ **Invariant 2 PASS.** Every field at every `push()` site is a real `tv_state` column. `TvController.jsx:203-206`'s `previewTv` is local-only and correctly **never** pushed. `tv_state` is only ever written at `useTvSync.js:32` — no bypass.
- ❌ **Invariant 3 FAIL — 4 divergences:**
  1. 🟡 **Progression reps are invisible on both TV paths.** `exVolStr` (`wod.js:73-74`) reads only `ex.reps`; `groupProgressionSteps` (`:83-85`) reads `s.reps || ex.reps`. For a progression exercise whose reps live only in `intensity.steps[].reps`, `exVolStr` returns `''` → `ExerciseList` renders **no volume at all** on the wall, only `"60/70/80 % RM"`. `ExRow.jsx:84-88` shows the same exercise *with* reps. **The gym wall loses the rep scheme the schedule shows.** Not the recorded Estações decision.
  2. 🔵 `bl.rounds`/`bl.duration` render on `BlockCard` (`slides.jsx:131,138`) but not on the TimerSlide right panel (`:253-260`) — an AMRAP shows "3 rds" on the WOD slide, nothing on the timer slide.
  3. 🔵 `_station` is dead data — `slides.jsx:129` attaches it; `ExerciseList.jsx` never reads it; TimerSlide's flatten (`:185`) doesn't attach it.
  4. 🔵 Empty WOD blocks diverge — `slides.jsx:75` filters them out of WodSlide; `:194` doesn't filter the TimerSlide group map.
- ⚠️ **Invariant 4 PARTIAL.** `tv_state`'s 16 columns in CLAUDE.md:69-88 match `0001_init.sql:177-195` **exactly**, verified line by line. **`timer_paused` confirmed documented-but-dead** (0 code hits) — CLAUDE.md's "confirm on schema dump" note can be resolved. **Not done:** exercise-shape diff against the *write* side (Criador/Exercicios/IntensityInput), the `exerciseRows`-dead-write-path claim, the registry `defaults{}` claim.
- ⚠️ **Invariant 5 — code is correct; the doc is stale.** Only **5** session-rendering public pages exist post-#52 (Index, Schedule, Results, Me, Leaderboard) and **all 5 filter correctly**. The 6th was `athletes.html`, retired. `Timer.jsx` reads no sessions; `TV.jsx` not filtering is the recorded TvController decision. **Fix CLAUDE.md 6→5, not the code.**
  - 🔵 One narrow gap: `Me.jsx:320` `.find(x => x.id === r.sessionId)` renders `s?.sessionName` at `:331` without the filter — a session made private *after* an athlete logged surfaces its name to that athlete only. Falls back to `'Treino'`. (`Results.jsx`'s `.find()` sites are safe — keyed off already-filtered ids.)
- 🔵 `blobTables.js:1-3` comment says "the **8** blob tables … shared by **Athletes.jsx** and Leaderboard.jsx" — there are **11** (`0001_init.sql:38-107`) and `src/public/athletes/` no longer exists. Destructuring is **correct** (`Leaderboard.jsx:81` `[sessions, athletes, , , , settings]` matches positions 0,1,5).
- 🔵 `Leaderboard.jsx:78` fetches all 8 `BLOB_TABLES`, uses 3 → **5 dead round-trips** per load (`events`, `locations`, `coach_profile`, `goals_data`, `lb_colors`). CLAUDE.md flags only the `lb_colors` slot.

## Dimension 5 — Security

Manual backend-surface pass + static sink/secret scan. (`/security-review` reviews a *diff*; this review has none — the manual pass is the right instrument for a BaaS app whose RLS is the whole defense.)

- ✅ **RLS surface is uniform and correct** on every one of the 14 tables: `public read` (SELECT `true`) + `auth write` (ALL, `is_allowed_user()`). **No anon write policy anywhere** — #7/#34's hardening holds.
- ✅ **`allowed_emails`: RLS enabled, 0 policies** = deny-all for anon/authenticated. #4's fix held — the allowlist `is_allowed_user()` trusts is not readable.
- ✅ **All 5 functions are `SECURITY DEFINER` with `search_path=""` pinned** (`is_allowed_user`, `submit_pr`, `clear_pr`, `class_checkin`, `log_result`).
- ✅ **No DOM sinks carrying user data** — the only `innerHTML` in the codebase is `gallery/MobileFrame.jsx:18` `doc.head.innerHTML = ''` (a clear, dev-only, no user data). Zero `dangerouslySetInnerHTML`/`eval`/`new Function`.
- ✅ **No secrets in client code** — 0 hits for service-role keys, and 0 hardcoded Supabase URLs/keys in `src/` (confirms #4's env migration held).
- ✅ `log_result` never touches `coach_note`/`flag_for_review`; `athlete_id` is in the conflict target, so an existing row keeps its owner. A caller-supplied `p_id` cannot overwrite another row (PK collision errors rather than overwrites).
- 🟡 **VERIFIED LIVE — `class_checkin`'s guest branch has no dedupe and no cap.** The athlete branch guards with `IF NOT (v_athlete_ids ? p_athlete_id)`; the guest branch appends unconditionally (`0003_anon_write_rpcs.sql`). Proven on the local stack — 5 identical anon calls each:

  ```
  athlete_ids: ["ath-123"]                                        ← 1 entry (dedupes)
  anon_names:  ["Fulano","Fulano","Fulano","Fulano","Fulano"]     ← 5 entries (no guard)
  ```

  Two problems, one cause: a guest double-tapping "Confirmar" appears **5× on the gym TV roster**, and it's an **unbounded anon-writable JSONB** with no cap and no rate limit. Nuance for the fix: two *real* guests can legitimately share a first name, so blind dedupe is not obviously right — likely wants a cap + a client-side guard, or dedupe-with-suffix. (Test row deleted; `class_executions` verified back to 0.)
- 🔵 Anon can call `log_result`/`submit_pr`/`class_checkin` for **any** athlete id — the honor-system gap. **Known and accepted** until #30/#31 (no athlete identity exists). Not a new finding; noted so the next reviewer doesn't re-file it.

## Dimension 6 — Performance

- **SPA main `index-*.js`: 423.99 kB (gzip 121.68)** — was 420.9 kB on 2026-07-05. **+3 kB over 5 sessions**; noise, not drift.
- Heavy deps all lazy ✓ — `jspdf.es.min` 399.18, `html2canvas` 199.56, `index.es` 151.41, `jspdf.plugin.autotable` 29.73, `purify.es` 26.09. Publicador itself 121.54.
- Public: largest page chunk **`schedule` 60.17 kB** (gzip 15.16), then me 43.44, tv 41.25, results 30.97, timer 30.69, leaderboard 10.17, index 9.06. Shared vendor: `week-*.js` **206.26 kB** + `jsx-runtime-*.js` **190.84 kB** (~400 kB of vendor across public pages — Rollup names shared chunks after an arbitrary member; these are supabase-js and React).
- CSS: SPA `index-*.css` **83.35 kB**; public `schedule` 33.41, `me` 23.46, `results` 18.66.
- 🟡 **Crimson Pro 600 is dead payload on every page** (see dim 2) — `fonts.js:13`; arguably the 400 roman too, since the single live `--font-body` site is italic.
- 🔵 `Leaderboard.jsx` makes 5 dead blob round-trips per load (dim 4).
- Build clean, 1.20s public. `gallery.html` confirmed absent from `public-dist/` ✓.

## Dimension 7 — Accessibility

**The tracked census in #14 is badly stale — it understates the fixes and mis-locates the gap.**

| Metric | #14 census (2026-07-05) | Actual now | Δ |
|---|---|---|---|
| `role=` | 3 | **28** | +25 |
| `aria-label=` | 2 | **34** | +32 |
| `<main>` | 0 | **7** | +7 |
| Headings | 1 | **19** (8 h1, 11 h2) | +18 |
| aria-* total | — | **133** | — |
| `onKey*` handlers | — | **24** | — |

- **The real gap is the SPA, not the public pages.** All **34** `aria-label`s are in `src/public/**`; `src/components/**` has **zero**, and only 2 `tabIndex`. Sessions #50/#51/#52 fixed the public side far beyond what #14 reflects.
- **onClick on non-interactive elements: 79** (not 60), across 23 files. **Critical methodology correction: 15 of the 79 are `onClick={e=>e.stopPropagation()}` propagation guards — not controls, and never should have been counted.** Split: `src/public/**` 31 (several verified *compliant* — `ExRow.jsx:54,98,132` have `role="checkbox"`+`aria-checked`+`tabIndex`+`onKeyDown`; `Results.jsx:409-412` likewise), **`src/components/**` 48** (Criador 14, Atletas 11, Exercicios 6, Servicos 5, Resultados 4, TvController 4). ⚠️ True violation count ≈ **49–60 but unverified** — the per-element classification did not complete.
- `<main>` **absent**: `timer/Timer.jsx`, `tv/TV.jsx`, `index/Index.jsx`, `recover/*`, and **all of `src/components/`** (App.jsx included).
- 🟡 **`Timer.jsx` has zero `aria-live` and zero `role="timer"`.** `role="timer"` appears **0×** codebase-wide. #14 stands for the timers.
- 🔵 **#14 overstates the leaderboard half** — `RankList.jsx:36` (`<ol aria-live="polite">`) already covers the live-updating lists. 9 `aria-live` exist, all in `src/public/**`.
- 🔵 Possible duplicate `<h1>` on me.html — `AthletePicker.jsx:53` and `HeroCard.jsx:34` both emit one; co-render not verified.

**Contrast (computed this pass — sRGB → linearize → relative luminance → (L1+0.05)/(L2+0.05)):**

| Pair | totk-dark | totk-light | spirit-blossom | sb-light |
|---|---|---|---|---|
| `--cream` on `--bg` | 16.05 ✅ | 14.81 ✅ | 16.26 ✅ | 16.65 ✅ |
| `--sub` on `--bg` | 9.42 ✅ | 8.87 ✅ | 8.85 ✅ | 10.79 ✅ |
| `--muted` on `--bg` | **3.75** ⚠️ | 4.59 ✅ | **3.26** ⚠️ | 6.08 ✅ |
| `--dim` on `--bg` | **2.27** ❌ | **2.49** ❌ | **1.75** ❌ | **3.60** ⚠️ |
| `--gold` on `--bg` | 8.98 ✅ | **3.79** ⚠️ | 5.57 ✅ | **4.49** ⚠️ |
| `--teal` on `--bg` | 9.65 ✅ | 5.37 ✅ | 9.51 ✅ | **3.34** ⚠️ |

✅ ≥4.5 body · ⚠️ 3–4.5 large/UI only · ❌ <3 fails both

- **`--dim` fails the 3:1 UI threshold in 3 of 4 themes** (1.75 in spirit-blossom). It is used for real text — e.g. `TV.module.css:20`'s loading/empty state.
- **`--muted` is large/UI-only on both dark themes** (3.75 / 3.26) — confirms #14's "4.0:1 fails body text" note, and the light themes pass.
- **New:** `--gold` on `--bg` drops to **3.79** (totk-light) / **4.49** (sb-light), and **`--teal` to 3.34** (sb-light) — gold/teal body text fails on the light themes. The light themes were never contrast-checked; both were added assuming the dark tuning carried over.

## Dimension 8 — Testing & quality gates

- **136 tests / 7 files**, green, 409ms. (CLAUDE.md says **5** files — drift; `goals.test.js` and `meHelpers.test.js` were added by #48/#52.)
- **Risk hotspots vs coverage** — the two biggest, most-churned files have **zero** tests: `Publicador.jsx` (2143) and `Criador.jsx` (1950). Every duplication fork in dim 3 is likewise untested, and `Schedule.jsx:207`'s local `getTargets` is the untested copy of a *tested* helper (`meHelpers.test.js:23-33`).
- CI gate: `npm test` runs before build in `deploy.yml` ✓. **Lint still not gated** — blocked on #32's 155 pre-existing problems.
- No Prettier / format gate (#24, correctly resized to M).
- 🔵 The gallery has **no CI protection by design** (dev-only, never built) — #52's hard-500 lived there undetected. Structural; the mitigation is "open it after touching it", already in CLAUDE.md.

## Dimension 9 — Docs & process hygiene

Drift found (all fixed inline this pass unless noted):

- `CLAUDE.md` "5 test files" → **7**.
- `CLAUDE.md` "all 6 public pages filter on this" → **5** (athletes.html retired in #52).
- `CLAUDE.md:129` "`var(--card)` is NOT defined — resolves to transparent. Use…" → **0 usages remain**; demote to history.
- `CLAUDE.md` `tv_state` `timer_paused` "(unused by code — confirm on schema dump, plans/04)" → **confirmed unused**; resolve the note.
- `blobTables.js:1-3` comment: "8 blob tables … Athletes.jsx and Leaderboard.jsx" → **11 tables**, Athletes.jsx gone. (Left for #60/#43 per CLAUDE.md's own instruction not to churn it.)
- Backlog line counts: #25 2152→**2143**, #26 1953→**1950**.
- Backlog **#14's entire census is stale** — see dim 7. Corrected.
- Backlog **#15**'s public-hex figure (93) → **~39 real**; radius full-src 230→**311**.

Plans checked: all 22 plan files carry correct Done markers or are live. `plans/16` (design program) is accurate post-#68. `plans/22`'s capture chain (#61–#67) is intact and #61 is correctly marked shipped.

---

## Triage → backlog

**New rows:** #69 (tv webfont), #70 (SPA canonical adoption), #71 (class_checkin guest cap), #72 (TV render-path divergence), #73 (dead-code sweep), #74 (Gallery/Resultados >800).
**Folded into existing rows:** TV/timer/index light-theme + hex/radius + QR palette + `BLOCK_COLORS` → **#53** (B4 owns those 4 pages). `--font-mono` → **#54**.
**Corrections:** #14 (census + SPA relocation), #15 (censuses), #25/#26 (line counts).
**Fixed inline:** CLAUDE.md test count, public-page filter count, `var(--card)`, `timer_paused`.

**This pass owes:** the **rest of dim 1** — mobile 390 across all pages, plus the results/me/timer journeys, none of which were re-driven after Docker died. Worth a targeted dim-1 pass before the next full one, since #50–#52 rewrote exactly those surfaces and dim 1 is where the sweeps are blind.
