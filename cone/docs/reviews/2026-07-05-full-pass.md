# App review — 2026-07-05 (third full pass)

Method: `/app-review` (9 dimensions; see [WORKFLOW.md](../WORKFLOW.md) "Review cadence"). Trigger: **Ready column emptied** after the #37→#38 chain shipped. Mechanical sweeps (dims 2/3/4/7) by parallel Explore agents; performance (6) run against a real `build:all`; security/testing/docs (5/8/9) by hand. **No code changed** — findings triaged into [BACKLOG.md](../BACKLOG.md) (rows referenced as #N). Legend: 🔴 high · 🟡 medium · ⚪ low · ✅ verified/resolved.

This is the **third** full pass ([first](./2026-07-02.md) 2026-07-02, [second](./2026-07-04-full-pass.md) 2026-07-04). The last pass was only ~1 day ago; the only thing shipped since is the **#37 → #38 chain** (load×distance volume, registry ghost defaults) plus the extraction of `src/components/shared/IntensityInput.jsx`. So this pass is narrow by design: confirm that chain's own resolutions and audit the surface it introduced.

## Not covered this session
- **Dim 1 — Visual/journey UX walk** — needs a driven session with eyes on the screen; still **#27**. This is the **3rd consecutive deferral** (2026-07-02, -04, -05) — it is now the most overdue dimension. See the recommendation in the Done entry.
- `/security-review` is diff-scoped, not whole-codebase — manual pass done instead (as in both prior reviews).

## Resolved by the #37/#38 chain ✅
- **`fmtIntensity` folded ×2 → ×1** — the diverged Publicador copy and the identical Schedule copy are gone; only `wod.js:64` remains, imported by `Publicador.jsx:10`, `Schedule.jsx:7`, `ExerciseList.jsx:1`. (The exact "#17 formatter half" #37 promised.)
- **`exVolStr` folded ×3-diverged → ×1** — the local copies in `Schedule.jsx`/`Publicador.jsx`/`Resultados.jsx` are deleted and re-imported from `wod.js:20`.
- **`IntensityInput` is a genuine shared extraction** — one 130-line component (`src/components/shared/IntensityInput.jsx`) consumed by both `Criador.jsx:13` (ghost props) and `Exercicios.jsx:5` (plain); ghost/plain split by optional props, no copy-paste divergence, zero new duplication. Builds as its own 4.74 kB lazy chunk.
- **`buildBenchmarkBlock` dist-drop fixed** — `benchmarks.js:53` now threads `dist`/`distUnit` (the #37 `/code-review` catch is in place).
- **`_presenterLogUrl` is no longer dead** — `Publicador.jsx:1751` is now consumed at `:1754` (`logUrl` → PresenterView). Drop it from #16's dead-code list.

## 1 · Product / UX
- Deferred — see "Not covered". No headless smoke regressions.

## 2 · Design consistency
- ✅ **Public hex flat at 93, border-radius flat at 92** (delta 0/0 vs 2026-07-04). No public `border-radius` added; no `var(--card)`; no non-pt-BR strings; no new 500/800 font-weights.
- ⚪ **~4 new hardcoded hex in `src/index.css`** for the ghost-suggestion styling: `#d8a840`/`rgba(216,168,64,…)` (`:113-114`, should be `var(--gold)`) and `#40b878`/`#1a4a2a` on `.ex-dist-toggle.on` (`:748`). SPA-side, matches the file's existing hardcoded-pill pattern. → **#28** (SPA token debt), cross-note **#15**.
- ✅ `IntensityInput.jsx` hex (`:42/72/87`) is a **net-zero move** out of Criador (verbatim, removed there same diff).
- ⚪ TV.module.css stragglers still present (`#000` `.blank:14`, `#c84038` `.countdownPill:127`). Unchanged → **#15**.

## 3 · Code quality
- 🟡 **File-size census: all six >800-line files GREW; none decomposed.** Publicador `2114→2152`, **Criador `1829→1953` (+124 NET despite pulling out the ~130-line IntensityInput)**, Schedule `1203→1265`, Resultados `936→980`, Me `881→936`, Atletas `781→829`. The extraction savings were fully eaten by new code (`normalizeCardioEx`, benchmark cardio normalization, ghost/materialize logic). → line-count corrections into **#25** (Publicador) / **#26** (Criador); the Criador-grew-despite-extraction point strengthens #26.
- 🔴 **`toSecs` NaN guard still missing** at `Atletas.jsx:22` and `Me.jsx:30` (mm:ss branch `parseInt(p[0])*60+parseInt(p[1])`, no per-part `||0`; canonical `wod.js:35` has it). Latent — malformed time → `NaN`. Pre-existing, on **#16**.
- 🟡 **`rankResults` still ×2** — local `Resultados.jsx:91` vs canonical `wod.js:46`. → **#16**.
- ⚪ **Duplication families (updated counts):** `fmtSecs` mm:ss reimplemented ×7 non-canonical (`Atletas:27`, `Me:31`, `Timer:16`, `slides:43`, `Athletes:115`, `TvController:375`, `Criador:103`); `DAY_PT` ×6 non-canonical (**was ×7** — correct #16) with a Titlecase-vs-UPPERCASE hazard (locals `['Dom'…]` vs canonical `['DOM'…]`, not drop-in); `fmtDate` ×5. None introduced by the chain. → **#16**.
- ⚪ **`tvRef` param still dead** (`useLiveRegistration.js:9`, still passed from `TvController.jsx:118`). → **#16**.
- ⚪ **Legacy-cardio dual representation** — `Criador.jsx:38-43` (`normalizeCardioEx`) strips `mode:'cardio'` on load, but `wod.js:26`, `Publicador.jsx:224/456/534` and the seed `benchmarks.js:9/28/36/38` still read/emit it. Coherent permanent legacy support (by design in #37), not dead code — noted so a future "remove cardio mode" cleanup knows the full surface. → note under **#17**.

## 4 · Architecture & contracts
- ✅ **No dual-client breach from the chain (the critical check).** `IntensityInput.jsx:1` imports **only `react`** — a two-hop breach is impossible (no second hop). Boundary sweep confirms `supabaseClient.js` is imported by 8 files, all under `src/public/**`; zero under `src/components/**`. The #41 fix holds: `TvController.jsx:5` imports slides from `slides.jsx` (client-free), not `TV.jsx`; the public client lives only at `TV.jsx:2`.
- ✅ **`exVolStr` canonical-only** across all 3 render paths (TV BlockCard + TimerSlide via `ExerciseList`, Schedule row `:329`); dist renders consistently.
- ✅ **`push()` still patch-only** — `useTvSync.js:32` spreads only `patch`; all ~20 call sites write real `tv_state` columns only.
- ✅ **`defaults` is JSONB-blob-only** — `Exercicios.jsx:149` writes `newEx.defaults` via `saveRegistry` (`utils/storage`); no new table/column assumed.
- ⚪ **Second bespoke dist formatter** at `Schedule.jsx:401` (progression-steps grouping path) — byte-identical output to `exVolStr`'s dist branch today, but duplicated logic that won't track a future `exVolStr` change. The #17 non-adoption, display-only. → note under **#17**.

## 5 · Security
- ✅ Client-side clean: no `service_role`/`SERVICE_ROLE`, no `dangerouslySetInnerHTML`/`innerHTML`/`eval` in `src/`. The chain added **no** new RLS/auth surface (blob-only edits), no new RPC/table, no new user-string DOM sink — React escaping still covers all athlete-entered strings.
- Live RLS probing already done in [2026-07-03-rls-probe.md](./2026-07-03-rls-probe.md); #7 (`0003`) + #34 (`0004`) hardening unchanged.

## 6 · Performance
- ✅ **No regression.** `build:all` green. **SPA main chunk flat at 420.91 kB** (2026-07-04 baseline 420 kB). Largest public page chunk `schedule` **58.41 kB** (was ~59). `IntensityInput` extraction is a clean 4.74 kB chunk; `slides` (16.78 kB) stays separate from `TvController` (34.40 kB), confirming #41's decoupling at the bundle level.
- Startup-fetch consolidation remains **#22**.

## 7 · Accessibility
- 🟡 **NEW, in the shared `IntensityInput` (so the gap doubles across Criador + Exercícios):**
  - `IntensityInput.jsx:56-59` — "sugestão ✕" dismiss is a **mouse-only `<span>`** (no `role`/`tabIndex`/`onKeyDown`/`aria-label`); keyboard/AT users can't reach or fire it.
  - `IntensityInput.jsx:102` — icon-only `ti-minus` "remove série" button, **no accessible name**.
  - Unit `<select>` with no accessible name: `Exercicios.jsx:391` (Cargas padrão) + `IntensityInput.jsx:91/114`.
  - ⚪ Cargas padrão inputs placeholder-labeled only (`Exercicios.jsx:384/389/397`); IntensityInput fields use `<span class="lbl">` not real `<label htmlFor>` (`:76/88-89/121`); Criador ghost qty conveyed by faint placeholder only (`:518/526/537`, but auto-materializes on save so not functionally blocking). → all into **#14**.
- ✅ **Mode tabs are correct** — real `<button type="button">` with text names (`% RM`/`Progressão`/`M/F`, `IntensityInput.jsx:66`); `+ Série` has a visible label.
- Correction to **#14**: its `TV.jsx:240` line ref is **stale** (current `TV.jsx` is 126 lines); the countdown-pill `role="timer"`/`aria-live` gap itself is still open (grep finds zero `aria-live`/`role="timer"` in all of `src`).

## 8 · Testing & gates
- ✅ CI test gate active; **83 tests / 5 files** (up from 79). #37 added `exVolStr` dist/legacy/regression cases.
- 🟡 **New untested logic from #38:** `materializeEx`/`materializeBlocks` (Criador ghost→real fill) and `normalizeCardioEx` (legacy migration) are pure-ish and unit-testable but uncovered. → append to **#23** (alongside `advanceAll`/`writeEntry`/`push()`/`fmtIntensity`).
- ⚪ Lint gate **#32** (155 pre-existing) and Prettier **#24** still deferred.

## 9 · Docs & process hygiene (fixed inline this session)
- **CLAUDE.md** "Shared rendering" note (`:95`) was **stale** — it claimed local `fmtIntensity` copies still live in Schedule.jsx (identical) and Publicador.jsx (diverged); #37 deleted both. Rewritten to reflect canonical-only. Exercise-data-shapes block gains `dist`/`distUnit` siblings and a note that `cardio` mode is now legacy-only.
- **BACKLOG**: this report linked into the header; **#17** unblocked (its "do #37 first" gate cleared) and **#16** promoted to Ready with plan files (`plans/13`, `plans/14`); corrections folded into #14 (new IntensityInput a11y gaps + stale TV.jsx line), #16 (DAY_PT ×7→×6, `_presenterLogUrl` no longer dead, toSecs severity), #23 (materialize/normalize untested), #25/#26 (new line counts), #28/#15 (index.css hex).

## Process notes for next review
- The mechanical sweeps (dims 2/3/4/7) are near-flat when only one small feature chain shipped — a **targeted pass** (just the touched dimensions) would have sufficed; reserve the full 9-dim pass for after an L item or a longer gap.
- Dim 1 (UX walk) has now deferred 3× — it should be the next *review-driven* work regardless of the feature backlog (it is #27, L/Opus, and also carries the #7 anon self-log click-through).
