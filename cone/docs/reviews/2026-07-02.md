# App review — 2026-07-02 (first full pass)

Method: `/app-review` (9 dimensions; see WORKFLOW.md "Review cadence"). Mechanical sweeps by parallel agents; build + security + docs by hand. **No code changed** — findings triaged into BACKLOG.md (rows referenced as #N). Legend: 🔴 high · 🟡 medium · ⚪ low.

## Not covered this session
- **Visual/journey UX walk** — all 9 public pages serve 200 on the dev server (smoke only). The judgment walk (17 screens × 2 viewports, 3 journeys) needs an interactive session with eyes on the screen → **#27**.
- **Live RLS probing** (can anon UPDATE another athlete's `results_v2` row?) — deferred to the dev environment, never against prod → carried inside plans/04 step 6.
- `/security-review` skill is diff-scoped (reviews branch changes), not whole-codebase — manual pass done instead.

## 1 · Product/UX
- ⚪ Smoke: all 9 public pages 200 under `/CrossFit-Apps/` base on `dev:public`. Visual walk deferred (#27).

## 2 · Design consistency
- 🔴 **`border-radius` rule broken 103×** in `src/public/**` (92 in CSS modules + 11 inline JSX; worst: Schedule.module.css 38, Athletes.module.css 18, TV.module.css 9). Many are `50%` circles — the rule likely needs a "circular indicators exempt" refinement, then a sweep (#15).
- 🔴 **93 hardcoded hex** in `src/public/**`. Worst: Leaderboard.jsx 17 (incl. off-palette cyan `#00b8d4` ×6 that should be `--teal`), Schedule.jsx check-in modal ~1234–1277 (8 off-palette), Me.jsx category palettes (13) (#15). TV.module.css hex→var already planned in plans/02.
- 🟡 tv.html hardcodes `theme-totk-dark` + `#0d0b09` literals, ignoring `cone_theme` — all 8 other pages have the theme-init script (covered by plans/02 acceptance).
- ⚪ Timer digits use `'Courier New',monospace` (TV.module.css:112,188,204,245) — no `--font-mono` token exists; decide and tokenize (#15).
- ⚪ `var(--card)`: zero usages — the CLAUDE.md warning can stay as-is.

## 3 · Code quality
- 🔴 **WOD-type lists hand-rolled in 5 pages** (Me, Index, Leaderboard, Athletes, Results) all **omit `Benchmark`** while canonical `WOD_TYPES`/`isWodBlock` (wod.js:3) include it — Benchmark blocks (added in #1) are likely invisible to results/leaderboard/PR logic on those pages. Canonical export is imported by no one (#6).
- 🟡 `fmtIntensity`: Schedule.jsx:14 copy is identical to canonical (safe delete); **Publicador.jsx:14 copy diverged** — has a `cardio` branch and `'%'→'% RM'` normalize the canonical lacks; reconcile into wod.js before consolidating (#17).
- 🟡 Dual canonical date utils: `storage.js:64,70` re-exports `toISO`/`todayISO` duplicating week.js; `uid` ×3 homes; SPA imports one, public the other (#16).
- 🟡 Formatter copies: `fmtSecs`-family ×5 (LiveRegistrationPanel's drops zero-padding), `toSecs` ×2 missing the `||0` NaN guard (Atletas.jsx:22, Me.jsx:29), `fmtDate` ×5, title-case `DAY_PT`/abbrev `MON_PT` ×6 with no canonical home (#16).
- 🟡 `useIsMobile`: real hook (matchMedia, 600px) + two divergent inline copies (innerWidth, 800px) in Resultados.jsx:55, Publicador.jsx:1134 (#16).
- 🟡 Oversized files: Publicador 2197, Criador 1952, Schedule 1286, Resultados 990, Me 935, Atletas 829. Publicador/Criador have clean seams (export views / mobile views / agenda / shell; editors / pickers / container) (#25, #26).
- ⚪ Dead code: none found (no orphan components/CSS modules/commented blocks). Two unrelated components both named `BlockTypePicker` (Criador inline vs public/timer) — rename candidate (#16).

## 4 · Architecture & contracts
- 🔴 **tv_state contract doc drift**: all 5 push sites + both readers use `timer_block_id`; CLAUDE.md/memory documented `block_id` and a `timer_paused` column no code touches. Code is internally consistent and prod TV works → docs corrected this session. Real schema should be confirmed when plans/04 dumps it.
- 🔴 **Undocumented anon write surface**: public check-in updates `class_executions` (Schedule.jsx:701,705) — contradicts the "anon writes = results_v2 only" rule. RLS on `class_executions` must allow it (or check-in silently fails); either way the rule and the policy need review (#7, probe via dev env).
- ✅ Client separation clean (hooks all SPA-side, only TvController imports them); `session.public !== false` filter correct on all 6 public pages; TvController correctly unfiltered.
- ✅ Page whitelist: all 9 pages present in `vite.public.config.js` inputs — but CLAUDE.md pointed at a deploy.yml whitelist that no longer exists (corrected this session).
- ⚪ TvController.jsx:238 hardcodes `/CrossFit-Apps/tv.html` base path.

## 5 · Security
- ✅ No `service_role`, no `dangerouslySetInnerHTML`/`innerHTML`/`eval` in `src/` — React escaping covers athlete-entered strings on all render paths.
- 🔴 **jsPDF loaded from cdnjs at runtime** (Publicador.jsx:913-914) — third-party script injection surface + fails offline + version pinned outside package.json, while `jspdf`+`jspdf-autotable` npm deps sit unused. Bundle it lazily like html2canvas (#8).
- 🟡 Anon-write surface: `results_v2` INSERT/UPDATE (intended) + `class_executions` UPDATE (undocumented, #7). Row-scope probe deferred to dev env (plans/04).
- 🟡 Anon key hardcoded in both clients — expected posture (key is public by design), moves to env vars with plans/04.

## 6 · Performance
- ✅ Public pages well split: largest page chunk 59 kB (schedule), react 190 kB, supabase 202 kB, shared per page. html2canvas already dynamic-imported.
- 🟡 SPA main chunk 420 kB + Publicador 123 kB — acceptable for a coach-only tool; decomposition (#25/#26) will help more than tuning.
- 🟡 Font weights **500/800 are used** (~19 declarations) **but never loaded** (fonts.js loads Cinzel 400/600/700/900, Crimson Pro 400/600/italic, Amarante 400) — browser fakes them; snap usages to loaded weights or load the real files (#15).
- ⚪ Earlier "eager font loading" concern is moot: @fontsource declares `@font-face`; browsers only download faces actually rendered.

## 7 · Accessibility
- 🔴 **Zero `aria-live`** in the tree — timer countdown, TV slides and leaderboard updates are silent to screen readers (#14).
- 🟡 ~32 icon/glyph-only buttons without `aria-label`/`title` (Schedule 10, Atletas 8, Me 2, Timer 2, Results 2, others) (#14).
- 🟡 No `<main>` landmark on any public page; only Header/Nav provide structure (#14).
- Contrast (computed): `--sub #c8b090` on `--bg` ≈ 9.4:1 ✅; `--muted #806850` on `--bg` ≈ 4.0:1 ❌ below 4.5:1 for body text (fine for decorative/large); `--dim #554a3a` ≈ 2.2:1 — decorative only (#14 documents allowed roles).

## 8 · Testing & gates
- 🔴 **CI deploys without running tests or lint** — deploy.yml: `npm ci` → `build:all` → publish; `npm test`/`npm run lint` exist but gate nothing (#5).
- 🟡 4 unit test files cover pure helpers only; the risk hotspots (push() patching, rotation advance, fmtIntensity modes — captured as #23) and all components are untested.
- ⚪ No Prettier/format enforcement (#24).

## 9 · Docs & process hygiene (fixed inline this session)
- CLAUDE.md: `src/lib/` → actually `src/public/lib/`; deploy whitelist → actually `vite.public.config.js` inputs; fmtIntensity note rewritten (canonical exists; ExerciseList adopted by TV, Schedule+Publicador are the holdouts); tv_state column list corrected (`timer_block_id`, drop unused `timer_paused`); themes.css location (repo root) documented; test count 3→4.
- BACKLOG: renumbered #5–#31 (dev env inserted as #4 Ready; old #5 mobile pass merged into #27 UX walk). Old FOUC item largely answered by this review — only the tap-flash remains (#9). Old perf item split: html2canvas already lazy, fonts moot, jsPDF → #8, startup fetches → #22. #17 rewritten to actual remaining scope.
- plans/02 and plans/03 checked — still valid (02's hex→var and tv.html theme-init confirmed as real needs).

## Process notes for next review
- `/security-review` needs a whole-codebase mode or manual substitute (as done here).
- The UX walk is the one dimension that can't run headless — pair it with a user session.
- Root-level loose files (`themes.css`, `cone-client.js`, `cone-utils.js`, `config.json`, ~30 mockups) live outside `cone/` — consider consolidating when touching deploy (#29 Vercel migration is the natural moment).
